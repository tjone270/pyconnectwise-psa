# coding: utf-8

"""
    ConnectWise PSA

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: 2022.2
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json


from typing import Any, ClassVar, Dict, List, Optional, Union
from pydantic import BaseModel, StrictFloat, StrictInt, StrictStr, field_validator
from pydantic import Field
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class PortalCalendar(BaseModel):
    """
    PortalCalendar
    """ # noqa: E501
    info: Optional[Dict[str, StrictStr]] = Field(default=None, alias="_info")
    adjust1_end: Optional[StrictStr] = Field(default=None, alias="adjust1End")
    adjust1_hours: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, alias="adjust1Hours")
    adjust1_start: Optional[StrictStr] = Field(default=None, alias="adjust1Start")
    adjust2_end: Optional[StrictStr] = Field(default=None, alias="adjust2End")
    adjust2_hours: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, alias="adjust2Hours")
    adjust2_start: Optional[StrictStr] = Field(default=None, alias="adjust2Start")
    adjust3_end: Optional[StrictStr] = Field(default=None, alias="adjust3End")
    adjust3_hours: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, alias="adjust3Hours")
    adjust3_start: Optional[StrictStr] = Field(default=None, alias="adjust3Start")
    id: Optional[StrictInt] = None
    week_start: Optional[StrictStr] = Field(alias="weekStart")
    __properties: ClassVar[List[str]] = ["_info", "adjust1End", "adjust1Hours", "adjust1Start", "adjust2End", "adjust2Hours", "adjust2Start", "adjust3End", "adjust3Hours", "adjust3Start", "id", "weekStart"]

    @field_validator('week_start')
    def week_start_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'):
            raise ValueError("must be one of enum values ('Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday')")
        return value

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of PortalCalendar from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
            },
            exclude_none=True,
        )
        # set to None if adjust1_hours (nullable) is None
        # and model_fields_set contains the field
        if self.adjust1_hours is None and "adjust1_hours" in self.model_fields_set:
            _dict['adjust1Hours'] = None

        # set to None if adjust2_hours (nullable) is None
        # and model_fields_set contains the field
        if self.adjust2_hours is None and "adjust2_hours" in self.model_fields_set:
            _dict['adjust2Hours'] = None

        # set to None if adjust3_hours (nullable) is None
        # and model_fields_set contains the field
        if self.adjust3_hours is None and "adjust3_hours" in self.model_fields_set:
            _dict['adjust3Hours'] = None

        # set to None if week_start (nullable) is None
        # and model_fields_set contains the field
        if self.week_start is None and "week_start" in self.model_fields_set:
            _dict['weekStart'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of PortalCalendar from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError("Error due to additional fields (not defined in PortalCalendar) in the input: " + _key)

        _obj = cls.model_validate({
            "_info": obj.get("_info"),
            "adjust1End": obj.get("adjust1End"),
            "adjust1Hours": obj.get("adjust1Hours"),
            "adjust1Start": obj.get("adjust1Start"),
            "adjust2End": obj.get("adjust2End"),
            "adjust2Hours": obj.get("adjust2Hours"),
            "adjust2Start": obj.get("adjust2Start"),
            "adjust3End": obj.get("adjust3End"),
            "adjust3Hours": obj.get("adjust3Hours"),
            "adjust3Start": obj.get("adjust3Start"),
            "id": obj.get("id"),
            "weekStart": obj.get("weekStart")
        })
        return _obj


