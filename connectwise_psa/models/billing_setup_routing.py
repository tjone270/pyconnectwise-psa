# coding: utf-8

"""
    ConnectWise PSA

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: 2022.2
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json


from typing import Any, ClassVar, Dict, List, Optional
from pydantic import BaseModel, StrictInt, StrictStr, field_validator
from pydantic import Field
from connectwise_psa.models.member_reference import MemberReference
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class BillingSetupRouting(BaseModel):
    """
    BillingSetupRouting
    """ # noqa: E501
    info: Optional[Dict[str, StrictStr]] = Field(default=None, alias="_info")
    id: Optional[StrictInt] = None
    invoice_rule: Optional[StrictStr] = Field(alias="invoiceRule")
    member: Optional[MemberReference] = None
    routing_rule: Optional[StrictStr] = Field(alias="routingRule")
    sequence_number: Optional[StrictInt] = Field(alias="sequenceNumber")
    __properties: ClassVar[List[str]] = ["_info", "id", "invoiceRule", "member", "routingRule", "sequenceNumber"]

    @field_validator('invoice_rule')
    def invoice_rule_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('All', 'Standard', 'Project', 'Agreement'):
            raise ValueError("must be one of enum values ('All', 'Standard', 'Project', 'Agreement')")
        return value

    @field_validator('routing_rule')
    def routing_rule_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('Account', 'Territory', 'Creator', 'Department', 'Location', 'Member', 'Project', 'Sales'):
            raise ValueError("must be one of enum values ('Account', 'Territory', 'Creator', 'Department', 'Location', 'Member', 'Project', 'Sales')")
        return value

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of BillingSetupRouting from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
            },
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of member
        if self.member:
            _dict['member'] = self.member.to_dict()
        # set to None if invoice_rule (nullable) is None
        # and model_fields_set contains the field
        if self.invoice_rule is None and "invoice_rule" in self.model_fields_set:
            _dict['invoiceRule'] = None

        # set to None if routing_rule (nullable) is None
        # and model_fields_set contains the field
        if self.routing_rule is None and "routing_rule" in self.model_fields_set:
            _dict['routingRule'] = None

        # set to None if sequence_number (nullable) is None
        # and model_fields_set contains the field
        if self.sequence_number is None and "sequence_number" in self.model_fields_set:
            _dict['sequenceNumber'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of BillingSetupRouting from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError("Error due to additional fields (not defined in BillingSetupRouting) in the input: " + _key)

        _obj = cls.model_validate({
            "_info": obj.get("_info"),
            "id": obj.get("id"),
            "invoiceRule": obj.get("invoiceRule"),
            "member": MemberReference.from_dict(obj.get("member")) if obj.get("member") is not None else None,
            "routingRule": obj.get("routingRule"),
            "sequenceNumber": obj.get("sequenceNumber")
        })
        return _obj


