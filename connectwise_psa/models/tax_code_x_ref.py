# coding: utf-8

"""
    ConnectWise PSA

    No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

    The version of the OpenAPI document: 2022.2
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json


from typing import Any, ClassVar, Dict, List, Optional
from pydantic import BaseModel, StrictBool, StrictInt, StrictStr, field_validator
from pydantic import Field
from connectwise_psa.models.tax_code_reference import TaxCodeReference
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class TaxCodeXRef(BaseModel):
    """
    TaxCodeXRef
    """ # noqa: E501
    info: Optional[Dict[str, StrictStr]] = Field(default=None, alias="_info")
    default_flag: Optional[StrictBool] = Field(default=None, alias="defaultFlag")
    description: StrictStr = Field(description=" Max length: 50;")
    id: Optional[StrictInt] = None
    level_five: Optional[StrictStr] = Field(default=None, alias="levelFive")
    level_four: Optional[StrictStr] = Field(default=None, alias="levelFour")
    level_one: Optional[StrictStr] = Field(default=None, alias="levelOne")
    level_six: Optional[StrictStr] = Field(default=None, alias="levelSix")
    level_three: Optional[StrictStr] = Field(default=None, alias="levelThree")
    level_two: Optional[StrictStr] = Field(default=None, alias="levelTwo")
    tax_code: Optional[TaxCodeReference] = Field(default=None, alias="taxCode")
    taxable_levels: Optional[List[StrictInt]] = Field(default=None, alias="taxableLevels")
    __properties: ClassVar[List[str]] = ["_info", "defaultFlag", "description", "id", "levelFive", "levelFour", "levelOne", "levelSix", "levelThree", "levelTwo", "taxCode", "taxableLevels"]

    @field_validator('level_five')
    def level_five_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('NonTaxable', 'Taxable'):
            raise ValueError("must be one of enum values ('NonTaxable', 'Taxable')")
        return value

    @field_validator('level_four')
    def level_four_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('NonTaxable', 'Taxable'):
            raise ValueError("must be one of enum values ('NonTaxable', 'Taxable')")
        return value

    @field_validator('level_one')
    def level_one_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('NonTaxable', 'Taxable'):
            raise ValueError("must be one of enum values ('NonTaxable', 'Taxable')")
        return value

    @field_validator('level_six')
    def level_six_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('NonTaxable', 'Taxable'):
            raise ValueError("must be one of enum values ('NonTaxable', 'Taxable')")
        return value

    @field_validator('level_three')
    def level_three_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('NonTaxable', 'Taxable'):
            raise ValueError("must be one of enum values ('NonTaxable', 'Taxable')")
        return value

    @field_validator('level_two')
    def level_two_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('NonTaxable', 'Taxable'):
            raise ValueError("must be one of enum values ('NonTaxable', 'Taxable')")
        return value

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of TaxCodeXRef from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
            },
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of tax_code
        if self.tax_code:
            _dict['taxCode'] = self.tax_code.to_dict()
        # set to None if default_flag (nullable) is None
        # and model_fields_set contains the field
        if self.default_flag is None and "default_flag" in self.model_fields_set:
            _dict['defaultFlag'] = None

        # set to None if level_five (nullable) is None
        # and model_fields_set contains the field
        if self.level_five is None and "level_five" in self.model_fields_set:
            _dict['levelFive'] = None

        # set to None if level_four (nullable) is None
        # and model_fields_set contains the field
        if self.level_four is None and "level_four" in self.model_fields_set:
            _dict['levelFour'] = None

        # set to None if level_one (nullable) is None
        # and model_fields_set contains the field
        if self.level_one is None and "level_one" in self.model_fields_set:
            _dict['levelOne'] = None

        # set to None if level_six (nullable) is None
        # and model_fields_set contains the field
        if self.level_six is None and "level_six" in self.model_fields_set:
            _dict['levelSix'] = None

        # set to None if level_three (nullable) is None
        # and model_fields_set contains the field
        if self.level_three is None and "level_three" in self.model_fields_set:
            _dict['levelThree'] = None

        # set to None if level_two (nullable) is None
        # and model_fields_set contains the field
        if self.level_two is None and "level_two" in self.model_fields_set:
            _dict['levelTwo'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of TaxCodeXRef from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError("Error due to additional fields (not defined in TaxCodeXRef) in the input: " + _key)

        _obj = cls.model_validate({
            "_info": obj.get("_info"),
            "defaultFlag": obj.get("defaultFlag"),
            "description": obj.get("description"),
            "id": obj.get("id"),
            "levelFive": obj.get("levelFive"),
            "levelFour": obj.get("levelFour"),
            "levelOne": obj.get("levelOne"),
            "levelSix": obj.get("levelSix"),
            "levelThree": obj.get("levelThree"),
            "levelTwo": obj.get("levelTwo"),
            "taxCode": TaxCodeReference.from_dict(obj.get("taxCode")) if obj.get("taxCode") is not None else None,
            "taxableLevels": obj.get("taxableLevels")
        })
        return _obj


